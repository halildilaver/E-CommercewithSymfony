<!DOCTYPE HTML>
<html>
<head>
    <title>{% block title %}Comment{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="keywords" content="{% block keywords %}{% endblock %}" />
    <script type="application/x-javascript"> addEventListener("load", function() { setTimeout(hideURLbar, 0); }, false); function hideURLbar(){ window.scrollTo(0,1); } </script>
    <!-- Bootstrap Core CSS -->
    <link href="{{ asset('admin') }}/css/bootstrap.min.css" rel='stylesheet' type='text/css' />
    <!-- Custom CSS -->
    <link href="{{ asset('admin') }}/css/style.css" rel='stylesheet' type='text/css' />
    <link rel="stylesheet" href="css/morris.css" type="text/css"/>
    <!-- Graph CSS -->
    <link href="{{ asset('admin') }}/css/font-awesome.css" rel="stylesheet">
    <!-- jQuery -->
    <script src="{{ asset('admin') }}/js/jquery-2.1.4.min.js"></script>
    <!-- //jQuery -->
    <link href='//fonts.googleapis.com/css?family=Roboto:700,500,300,100italic,100,400' rel='stylesheet' type='text/css'/>
    <link href='//fonts.googleapis.com/css?family=Montserrat:400,700' rel='stylesheet' type='text/css'>
    <!-- lined-icons -->
    <link rel="stylesheet" href="{{ asset('admin') }}/css/icon-font.min.css" type='text/css' />
    <!-- //lined-icons -->
    <!-- tables -->
    <link rel="stylesheet" type="text/css" href="{{ asset('admin') }}/css/table-style.css" />
    <link rel="stylesheet" type="text/css" href="{{ asset('admin') }}/css/basictable.css" />
    <link rel="stylesheet" type="text/css" href="{{ asset('admin') }}/css/tab.css" />
    <script type="text/javascript" src="{{ asset('admin') }}/js/jquery.basictable.min.js"></script>
</head>
<body>
    <h1>Comments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ comment.userid }}</td>
            </tr>
            <tr>
                <th>Productid</th>
                <td>{{ comment.productid }}</td>
            </tr>
            <tr>
                <th>User Name</th>
                <td>{{ comment.name }}</td>
            </tr>
            <tr>
                <th>Product Name</th>
                <td>{{ comment.productname }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ comment.comment }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td><form action="{{ path('comments_edit', {'id': comment.id}) }}" method="post">
                        <input type="hidden" name="comments[productid]" value="{{ comment.productid }}">
                        <input type="hidden" name="comments[comment]" value="{{ comment.comment }}">
                        <input type="hidden" name="comments[name]" value="{{ comment.name }}">
                        <input type="hidden" name="comments[productname]" value="{{ comment.productname }}">
                        <input type="hidden" name="comments[userid]" value="{{ comment.userid }}">
                <select name="comments[status]">
                        <option>{{ comment.status }}</option>
                        <option>Published</option>
                        <option>New</option>
                    </select>
                    <input type="submit" value="Güncelle">
                </form></td>
            </tr>
            <tr>
                <th>Delete</th>
                <td>{{ include('comments/_delete_form.html.twig') }}</td>
            </tr>
        </tbody>
    </table>

</body>